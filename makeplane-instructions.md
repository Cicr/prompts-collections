Plane have this Hierarchy.

1-Project --> Module --> Cycle --> work Item(Issue) --> sub work item
2-The sub work item is an Issue on its own, but have the following properties set.
2.1- Parent, This property is set to the work item. 
2.1- In relation the property is set to Relates to
2.2 - All properties like, Module, Cycle, Labels, relation are set the same as the parent


-There is a Property called parent. to link the sub-work item.
-There is a Property called.  Relation.
--Relates to
--Duplicate of
--Blocked by
--Blocking

when creating work Items or sub-work items use scrum standards add the following information.  use the title or description to generate the information.

Acceptance Criteria
Definition of Done
